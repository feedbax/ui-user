import styled from 'styled-components';
import { paginationDotStyle, wrapperStyle } from './styles';

export const PaginationDot = styled.div`
  ${paginationDotStyle}
`;

export const Wrapper = styled.div`
  ${wrapperStyle}
`;
