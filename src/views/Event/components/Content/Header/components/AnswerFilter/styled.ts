import styled from 'styled-components';
import { filterStyle, wrapperStyle } from './styles';

export const Filter = styled.div`
  ${filterStyle}
`;

export const Wrapper = styled.div`
  ${wrapperStyle}
`;
